{
  "name": "tablecraft",
  "version": "1.0.0",
  "private": true,
  "description": "Drizzle table query builder engine + Shadcn + Airtable = Complex table setup in 5 minutes instead of 1 hour.",
  "scripts": {
    "build": "bun run --filter '*' build",
    "test": "bun run --filter '*' test",
    "test:integration": "bun run --filter hono-example test:integration && bun run --filter sveltekit-example test:integration",
    "typecheck": "bun run --filter '*' typecheck"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "dependencies": {
    "@types/react": "^19.2.13"
  }
}
